<% content_for(:title, t('.title')) %>
<div class="container">
  <div class="row g-4">
    <h1 class="py-3 text-center"><%= t('.title') %></h1>
    <%= render 'form', { post: @post } %>
  </div>
</div>
<%= link_to bookmark_post_path(post.bookmarks.find { |b| b.user_id == current_user.id }),
  id: "js-bookmark-button-for-post-#{post.id}",
  class:"float-right",
  method: :delete,
  remote: true do %>
    <%= icon 'fas', 'star' %>
<% end %>